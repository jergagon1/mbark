<%= erb :_nav, :layout => false %>
<%= erb :_weather, :layout => false %>
<div id="main_content">
  <h1>Users</h1>
  <ul>
    <% @users.each do |user|  %>
    <li><%= "#{user.id} #{user.username} #{user.email} #{user.zipcode}" %></li>
    <% end  %>
  </ul>
</div>
