<%= erb :_nav, :layout => false %>
<%= erb :_weather, :layout => false %>
<div id="main_content">
  <h1 class="color_text">Users</h1>
  <ul class="users_list">
    <% @users.each do |user|  %>
    <li><%= "#{user.username} #{user.email} #{user.zipcode}" %></li>
    <% end  %>
  </ul>
</div>
